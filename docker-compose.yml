# version: "3.8"

# services:
#   web:
#     build:
#       context: .
#       dockerfile: Dockerfile
#     ports:
#       - "3030:3000"
#     environment:
#       NODE_ENV: production
#     command: npm start
version: "3"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3030:3000"
    restart: always
